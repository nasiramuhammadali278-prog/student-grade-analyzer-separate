# Student Grade Analyzer

A standalone student grade analyzer with an HTML frontend and FastAPI backend.

## Run the server

```bash
cd "sending and recieving"
pip install -r requirements.txt
uvicorn server:app --reload
```

Then open `htmls.html` in a browser and enter a score. Scores of 50 or higher pass.
